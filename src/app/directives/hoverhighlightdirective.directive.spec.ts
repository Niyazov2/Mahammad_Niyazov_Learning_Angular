import { HoverhighlightdirectiveDirective } from './hoverhighlightdirective.directive';

describe('HoverhighlightdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverhighlightdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
